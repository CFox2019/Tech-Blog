<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>

  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
</head>

<body>

  <header class="bg-dark">

    <div class="container">
      <nav class="navbar navbar-expand-lg bg-dark">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <div class="navbar-nav me-auto mb-2 mb-lg-0">
            <div class="nav-item">
              <a class="nav-link active" aria-current="page" href="/">Home</a>
            </div>
            <div class="nav-item">
              <a class="nav-link" href="/dashboard">Dashboard</a>
            </div>
            <!-- Conditionally render login or logout links -->
            {{#if logged_in}}
            <div class="nav-item">
              <a class="nav-link" id="logout" href="/logout">Logout</a>
            </div>
            {{else}}
            <div class="nav-item">
              <a class="nav-link" href="/login">Login</a>
            </div>
          </div>
          {{/if}}
        </div>
      </nav>

       <!-- Conditionally render Your Dashboard button or The Tech Blog button -->
      {{#if in_dashboard}}
      <h1>
        <button class="p-3 mb-5 ms-3" href="/dashboard">Your Dashboard</button>
      </h1>
      {{else}}
      <h1>
        <button class="p-3 mb-5 ms-3" href="/">The Tech Blog</button>
      </h1>
      {{/if}}
    </div>

  </header>

  <main class="container container-fluid mt-5">
    {{{ body }}}
  </main>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}

  <!-- Render script for in_dashboard only  -->
  {{#if in_dashboard}}
  <script src="/js/dashboard.js"></script>
  {{/if}}

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-JEW9xMcG8R+pH31jmWH6WWP0WintQrMb4s7ZOdauHnUtxwoG2vI5DkLtS3qm9Ekf"
    crossorigin="anonymous"></script>

</body>

</html>